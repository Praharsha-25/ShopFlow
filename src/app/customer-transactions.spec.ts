import { CustomerTransactions } from './customer-transactions';

describe('CustomerTransactions', () => {
  it('should create an instance', () => {
    expect(new CustomerTransactions()).toBeTruthy();
  });
});
